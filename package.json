{
  "name": "pinned-sidebar",
  "version": "2.1.7",
  "private": true,
  "description": "Local-first sidebar extension for Edge and Chrome",
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser.cjs",
    "format": "prettier --write *.js *.css *.html *.json tests/*.cjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
